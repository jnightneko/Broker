server:
  port: 8095

## mongodb de forma remota ##
spring:
  data:
    mongodb:
      uri: mongodb+srv://${MONGODB_USER}:${MONGODB_PASSWORD}@cluster0.jyukdpg.mongodb.net/
      database: DB

# mongodb de forma local
#spring:
 # data:
  #  mongodb:
   #   uri: mongodb://localhost:27017/DB

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka